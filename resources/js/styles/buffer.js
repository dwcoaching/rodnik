import { Stroke, Style, Circle, Fill } from 'ol/style'

export default new Style({
    // stroke: new Stroke({
    //     color: '#0000ff',
    //     width: 2,
    // }),
    fill: new Fill({
        color: 'rgba(0, 0, 255, 0.5)',
    }),
})
